{
  "name": "wpp-gpt",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn clean && tsup src/index.ts --sourcemap",
    "build:tsc": "yarn clean && tsc && tsc-alias",
    "start:watch": "tsup src/index.ts --watch --onSuccess \"yarn start\"",
    "start": "node dist"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.186",
    "@types/node": "^18.11.1",
    "rimraf": "^4.1.2",
    "tsc-alias": "^1.8.2",
    "tsup": "^6.5.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@wppconnect-team/wppconnect": "^1.20.0",
    "dotenv": "^16.0.3",
    "lodash": "^4.17.21",
    "openai": "^3.1.0"
  }
}
